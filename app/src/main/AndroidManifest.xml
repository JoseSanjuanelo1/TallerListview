<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.josesanjuanelo.tallerlistview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Principal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".registrarCarros" />
        <activity android:name=".ListarCarros" />
        <activity android:name=".Reportes" />
        <activity android:name=".CarrosRegistrados" />
        <activity
            android:name=".carroMasCaro"
            android:label="@string/title_activity_carro_mas_caro"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".carroMasEconomico"></activity>
    </application>

</manifest>